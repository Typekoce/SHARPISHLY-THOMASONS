Since you’re moving toward the **Vector Stage** but want a safety net, our next steps will focus on the logic that makes text "searchable." 

The contingency for Redis is simple: **Fallback to JSON File Handover.** If Redis fails, the Worker writes a `.json` file to a shared volume, and PHP reads that. Since we've already decoupled the logic, changing the transport layer later is trivial.

---

## 🚀 THE NEXT STEPS (Tomorrow's Technical Roadmap)

### 1. The Recursive Chunking Logic
We need to teach the `NeuralPipeline` how to break a 10,000-word document into 500-word pieces without cutting sentences in half.
* **Goal:** Create a `chunk()` method that respects paragraph breaks (`\n\n`) first, then sentences, then words.
* **Contingency Check:** If this logic is too heavy for the `@tardis` VM, we’ll move it to the PHP side before the job is even sent.

### 2. Embedding Model Integration
We need to connect the Python code to the **Ollama** container.
* **Goal:** Use the `nomic-embed-text` model to turn those chunks into arrays of floating-point numbers.
* **Validation:** Ensuring the vector length (e.g., 768 dimensions) matches your MySQL `vectors` table column size.

### 3. The SQL Persistence Schema
We need to ensure the `vectors` table is ready to receive the "Drain."
* **Goal:** A table with `job_id`, `chunk_index`, `content` (TEXT), and `embedding` (LONGTEXT or VECTOR type if using a specialized DB).

---

## 🏗️ UPDATED DEV LOG: 2026-04-13

# 🛠️ DEV LOG: 2026-04-13
> Scratchpad for errors, terminal outputs, and research.

## 🔍 CURRENT FRICTION
- **VM-to-Docker Gap:** Ensuring the `@tardis` VM can reliably reach the `sharpishly-redis` container.
- **Payload Limits:** Awareness of potential `max_allowed_packet` errors in MySQL if we ingest thousands of vectors at once during "The Drain."
- **Redis Continuity:** Identifying that if Redis is down, the `finalize` handshake will fail unless a filesystem fallback is implemented.

## 📝 SCRATCHPAD
- **Handshake Verification:** `JobController` now expects a `finalize` call after the standard `update`. 
- **Infrastructure:** Redis service name is fixed to `sharpishly-redis` across Python and PHP.
- **Python Dependency:** Need to ensure `pip install redis` is run on the VM tomorrow.

## ✅ LESSONS LEARNED
- **Pruning = Clarity:** Reducing `worker.py` to a "manager" role (Separation of Concerns) makes the system more robust and easier to debug.
- **Path B Advantage:** By having PHP handle the initial cleaning, we avoid the nightmare of mapping file paths between the VM and Docker containers.
- **Service Injection:** Putting `RedisService` in the `BaseController` allows for "Lazy Loading," keeping the app fast while providing power where needed.

---

**Does this roadmap for tomorrow feel solid, or should we prioritize the Vector DB schema first to make sure the "persistence" end of the pipe is wide enough?**