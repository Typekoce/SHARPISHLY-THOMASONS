name: sharpishly

services:
  # --- 1. The Broker (Redis) ---
  redis:
    image: redis:7-alpine
    container_name: sharpishly-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - sharpishly-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # --- 2. Database (MySQL 8.0) ---
  db:
    image: mysql:8.0
    container_name: sharpishly-db
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_ROOT_PASSWORD: ${DB_PASS}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - sharpishly-net
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s

  # --- 3. Semantic Memory (Neo4j) ---
  graph:
    image: neo4j:latest
    container_name: sharpishly-graph
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: ${NEO4J_USER}/${NEO4J_PASS}
    volumes:
      - ./database/neo4j/data:/data
    networks:
      - sharpishly-net

  # --- 4. The Backend (PHP-FPM) ---
  php:
    build: .
    container_name: sharpishly-php
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/var/www/html
      - ./storage:/var/www/html/storage:rw
      - ./infra/php/www.conf:/usr/local/etc/php-fpm.d/www.conf
    networks:
      - sharpishly-net
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy

  # --- 5. The Web Server (Nginx) ---
  web:
    image: nginx:alpine
    container_name: sharpishly-app
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - sharpishly-net
    depends_on:
      - php

  # --- 6. Neural Models (Ollama) ---
  llm:
    image: ollama/ollama:latest
    container_name: sharpishly-ollama
    restart: unless-stopped
    volumes:
      - ./storage/ollama:/root/.ollama
    networks:
      - sharpishly-net
    command: serve

  # --- 7. AI API & Worker Pool ---
  ai:
    build: ./ai
    container_name: sharpishly-ai-api
    restart: unless-stopped
    env_file: .env
    # Orchestration: Wait for Nginx, ping migrate, then start Uvicorn
    command: >
      sh -c "echo 'Waiting for web server...';
             while ! nc -z sharpishly-app 80; do sleep 1; done;
             echo 'Triggering PHP Migration...';
             curl -s http://sharpishly-app/php/scaffold/migrate;
             echo 'Starting AI API...';
             uvicorn server:app --host 0.0.0.0 --port 8000"
    networks:
      - sharpishly-net
    depends_on:
      - web

  worker:
    build: ./ai
    container_name: sharpishly-ai-worker
    restart: unless-stopped
    env_file: .env
    command: tail -f /dev/null
    networks:
      - sharpishly-net

networks:
  sharpishly-net:
    driver: bridge

volumes:
  mysql_data:
  redis_data: