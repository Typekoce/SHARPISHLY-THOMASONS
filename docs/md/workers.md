# Workers Infrastructure

All background jobs, process execution loops, and AI ingestion pipelines are managed via a single orchestrator entry point.

## Starting Workers

From the project root:

```bash
make workers
